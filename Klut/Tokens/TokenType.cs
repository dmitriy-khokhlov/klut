namespace Klut.Tokens
{
    enum TokenType
    {
        // 'None' must be the default value, so set this to zero.
        None = 0,
        Blank,
        Identifier,
        Semicolon,
        OpenParenthesis,
        CloseParenthesis,
        String,
    }
}
